{
  "name": "pdf-annotator",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild src/extension.ts src/bookmark.ts --bundle --outdir=dist --format=esm --sourcemap && cp manifest.json dist/ && cp src/styles.css dist/ && cp -R icons dist/",
    "watch": "esbuild src/extension.ts src/bookmark.ts --bundle --outdir=dist --format=esm --sourcemap --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
